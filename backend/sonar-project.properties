sonar.projectKey=litethinking-backend
sonar.projectName=LiteThinking Backend
sonar.projectVersion=1.0
sonar.organization=lucho118

# Fuentes a analizar (sin duplicar)
sonar.sources=apps,config
sonar.python.version=3.12

# Excluir migraciones, cache y build artifacts
sonar.exclusions=**/migrations/**,**/__pycache__/**,**/node_modules/**,**/*.egg-info/**

# Tests: usar inclusions en vez de sonar.tests (wildcards no soportados)
sonar.test.inclusions=apps/**/tests/**

# Cobertura
sonar.python.coverage.reportPaths=coverage.xml
